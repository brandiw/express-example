<div class="container">
  <h1>Oh hello</h1>
  <h2>Check out some movies I like</h2>

  <% movies.forEach(function(m){%>
    <div>
      <h3><%= m.Title %> (<%= m.Year %>)</h3>
      <img src="<%= m.Poster %>">
    </div>
  <%});%>

</div>
